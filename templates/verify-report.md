# Verify Report：验收报告（R 审查阶段产出）

> Issue: #<编号> | 阶段: [R] Review | 状态标签: `riper-review`
> 产出角色: QA（见 `roles/reviewer.md`）——**T0：禁改业务代码；可写区域仅 `test/` 与本报告**
> 输入: `plan.md`（冻结基线及其修订记录）

**审查原则**：只认 `plan.md` 中的验收标准，逐条判定 PASS / FAIL；不接受"代码能跑就算过"。
**验证方式**：偏向**黑盒**（CLI / API / 端到端 / 文件产物）；如需测试脚本，**默认生成到 `test/`**（除非用户显性指定其他路径）。

## 1. 逐项验收结果

### 步骤 1：<任务标题>

- **验收标准**: <摘自 plan.md>
- **验证方式**: <黑盒优先：跑了什么命令 / 调了什么接口 / 检查了什么产物；必要时补充只读代码审查>
- **结果**: **PASS** / **FAIL**
- **证据**: <命令输出摘要、文件/行号引用、测试脚本路径（如 `test/xxx.sh`）>

### 步骤 2：<任务标题>

- **验收标准**: <...>
- **验证方式**: <...>
- **结果**: **PASS** / **FAIL**
- **证据**: <...>

<!-- 按计划步骤逐项复制，不得遗漏 -->

## 2. 附加检查（不阻塞判定，仅记录）

- 代码规范 / lint:
- 提交信息与 Issue 关联:
- 是否存在计划外的越界改动:
- 新增测试脚本是否落在 `test/` 目录:

## 3. 汇总

| 指标 | 数值 |
|------|------|
| 总步骤数 | |
| PASS | |
| FAIL | |
| 重试轮次 | (当前第 x / 最多 3 次) |

## 4. 失败原因分析（仅 FAIL 时填写）

| 步骤 | 失败原因 | 根因分类（计划缺陷 / 实现缺陷 / 环境问题） | 建议的下一步 |
|------|----------|---------------------------------------------|--------------|

## 5. 结论

- [ ] **全部 PASS** → 状态切为 `riper-verified`，由 QA 关闭 Issue，结束循环
- [ ] **存在 FAIL** → 状态退回 `riper-plan`，PM 修订计划后重新走 [E] → [R]
- [ ] **重试已达 3 次仍有 FAIL** → 状态 `riper-blocked`，等待人工介入

---
*本报告全文将作为 Issue 评论写回。*
